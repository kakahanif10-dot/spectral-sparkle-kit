# Gradient button update and publish

## Changes
- Define one reusable blue-to-violet gradient using `#063CFF`, `#4F29FF`, and `#8C17FF` in the global design system.
- Apply it to all primary purple buttons while preserving readable text, focus states, and interaction feedback.
- Verify the result in the live page at desktop and mobile sizes.
- Publish the updated app and report the live URL.

## Technical details
- Keep colors centralized as semantic design tokens rather than repeating values across page code.
- Preserve the existing page structure and behavior; only button presentation changes.
- The provided GitHub repository cannot be pushed to directly from this workspace. The app will be published through Lovable; GitHub synchronization remains managed from the project’s GitHub integration.
